// examples/demo.ltl — runnable demo for ltl_select_demo

import std::io
use ltl_select_demo as lib

fn main() {
    io::println("ltl_select_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}